import * as UrlPattern from "url-pattern";

export const patternLogin = new UrlPattern("/login(/)");
export const patternDashboard = new UrlPattern("/dashboard(/)");
export const patternSignup = new UrlPattern("/signup(/)");
